name: Release

on: push

permissions:
  contents: write

jobs:
  guard:
    name: Release guard
    runs-on: ubuntu-latest
    steps:
      - run: echo "ref=${{ github.ref }} ref_type=${{ github.ref_type }}"
